import { compareElements } from '../helpers';

const fireCardFunction = (battle, setBattle, monster, computer, wave) => {
  console.log('fireCardFunctions');
};
const fireballCardFunction = (battle, setBattle, monster, computer, wave) => {
  console.log('fireballCardFunction');
};
const lavaCardFunction = (battle, setBattle, monster, computer, wave) => {
  console.log('lavaCardFunction');
};
const rainOfFireCardFunction = (battle, setBattle, monster, computer, wave) => {
  console.log('rainOfFireCardFunction');
};
const volcanoCardFunction = (battle, setBattle, monster, computer, wave) => {
  console.log('volcanoCardFunction');
};
const ashRainCardFunction = (battle, setBattle, monster, computer, wave) => {
  console.log('ashRainCardFunction');
};
const ignitionStrikeCardFunction = (
  battle,
  setBattle,
  monster,
  computer,
  wave
) => {
  console.log('ignitionStrikeCardFunction');
};
const pyroclasmCardFunction = (battle, setBattle, monster, computer, wave) => {
  console.log('pyroclasmCardFunction');
};
const fireWhipCardFunction = (battle, setBattle, monster, computer, wave) => {
  console.log('fireWhipCardFunction');
};
const burningMeteorCardFunction = (
  battle,
  setBattle,
  monster,
  computer,
  wave
) => {
  console.log('burningMeteorCardFunction');
};

export {
  fireCardFunction,
  fireballCardFunction,
  lavaCardFunction,
  rainOfFireCardFunction,
  volcanoCardFunction,
  ashRainCardFunction,
  ignitionStrikeCardFunction,
  pyroclasmCardFunction,
  fireWhipCardFunction,
  burningMeteorCardFunction,
};
