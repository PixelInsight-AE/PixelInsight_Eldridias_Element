const waterCardFunction = (battle, setBattle, monster, computer, wave) => {
  console.log('waterCardFunction');
};
const aquaSlashCardFunction = (battle, setBattle, monster, computer, wave) => {
  console.log('aquaSlashCardFunction');
};
const waterFallCardFunction = (battle, setBattle, monster, computer, wave) => {
  console.log('waterFallCardFunction');
};
const iceCardFunction = (battle, setBattle, monster, computer, wave) => {
  console.log('iceCardFunction');
};
const blizzardCardFunction = (battle, setBattle, monster, computer, wave) => {
  console.log('blizzardCardFunction');
};
const iceSpikeCardFunction = (battle, setBattle, monster, computer, wave) => {
  console.log('iceSpikeCardFunction');
};
const oceanCurrentCardFunction = (
  battle,
  setBattle,
  monster,
  computer,
  wave
) => {
  console.log('oceanCurrentCardFunction');
};
const highTideCardFunction = (battle, setBattle, monster, computer, wave) => {
  console.log('highTideCardFunction');
};
const coralCrushCardFunction = (battle, setBattle, monster, computer, wave) => {
  console.log('coralCrushCardFunction');
};
const tsunamiCardFunction = (battle, setBattle, monster, computer, wave) => {
  console.log('tsunamiCardFunction');
};
export {
  waterCardFunction,
  aquaSlashCardFunction,
  waterFallCardFunction,
  iceCardFunction,
  blizzardCardFunction,
  iceSpikeCardFunction,
  oceanCurrentCardFunction,
  highTideCardFunction,
  coralCrushCardFunction,
  tsunamiCardFunction,
};
