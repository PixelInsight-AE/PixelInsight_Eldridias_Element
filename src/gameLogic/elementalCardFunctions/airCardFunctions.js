const airCardFunction = (battle, setBattle, monster, computer, wave) => {
  console.log('airCardFunction');
};
const cloudCardFunction = (battle, setBattle, monster, computer, wave) => {
  console.log('cloudCardFunction');
};
const lightningCardFunction = (battle, setBattle, monster, computer, wave) => {
  console.log('lightningCardFunction');
};
const windCardFunction = (battle, setBattle, monster, computer, wave) => {
  console.log('windCardFunction');
};
const rainCardFunction = (battle, setBattle, monster, computer, wave) => {
  console.log('rainCardFunction');
};
const featherCardFunction = (battle, setBattle, monster, computer, wave) => {
  console.log('featherCardFunction');
};
const skyDiveCardFunction = (battle, setBattle, monster, computer, wave) => {
  console.log('skyDiveCardFunction');
};
const gustCannonCardFunction = (battle, setBattle, monster, computer, wave) => {
  console.log('gustCannonCardFunction');
};
const aerialAssaultCardFunction = (
  battle,
  setBattle,
  monster,
  computer,
  wave
) => {
  console.log('aerialAssaultCardFunction');
};
const hurricaneCardFunction = (battle, setBattle, monster, computer, wave) => {
  console.log('hurricaneCardFunction');
};

export {
  airCardFunction,
  hurricaneCardFunction,
  cloudCardFunction,
  lightningCardFunction,
  windCardFunction,
  rainCardFunction,
  featherCardFunction,
  skyDiveCardFunction,
  gustCannonCardFunction,
  aerialAssaultCardFunction,
};
