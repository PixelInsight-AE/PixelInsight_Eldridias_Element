import { motion } from 'framer-motion';
import { SignUpForm } from './_SignUpForm';

const HeroSix = (props) => {
  return (
    <section id="heroSix">
      <h1>What are you waiting for?</h1>
      <SignUpForm />
    </section>
  );
};

export { HeroSix };
