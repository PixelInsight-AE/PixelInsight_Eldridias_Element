import { motion } from 'framer-motion';
import { HeroOne } from './Home_HeroOne';

const HeroSix = (props) => {
  return (
    <section id="heroSix">
      <h1>What are you waiting for?</h1>
      <HeroOne />
    </section>
  );
};

export { HeroSix };
